'use strict';
// const error = require('../../../i18n/vi/error');
// const {
//   validateCode,
//   validatePhoneNumber,
//   // validateAddress,
//   validateEmail,
//   validateFaxNumber,
//   validateBankAccount,
// } = require('../../logic/validation/validate-methods');
// eslint-disable-next-line
module.exports = Client => {
  // Client.validateAsync('code', validateCode, { message: error.INVALID_DATA });
  // Client.validateAsync('phoneNumber', validatePhoneNumber, { message: error.INVALID_DATA });
  // Client.validateAsync('address', validateAddress, { message: error.INVALID_DATA });
  // Client.validateAsync('buyerEmail', validateEmail, { message: error.INVALID_DATA });
  // Client.validateAsync('buyerFaxNumber', validateFaxNumber, { message: error.INVALID_DATA });
  // Client.validateAsync('buyerBankAccount', validateBankAccount, { message: error.INVALID_DATA });
};
