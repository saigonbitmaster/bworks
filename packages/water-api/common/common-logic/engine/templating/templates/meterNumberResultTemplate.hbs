<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
        body {
          font: normal 14px Arial, sans-serif;
        }
        table {
          border-collapse: collapse;
          width: 80%;
          margin: auto;
        }
        table, th, td {
          border: 1px solid black
        }
        #title-block {
          margin: auto;
        }
    </style>
  </head>
  <body>
    <div>
      <h4>TRUNG TÂM NƯỚC SẠCH &amp; VSMT NT BẮC NINH</h4>
      <div id="title-block">
        <h1 id="title">BẢNG KÊ CHỈ SỐ ĐỒNG HỒ NƯỚC</h1>
        <h4>Tháng {{date.month}}/{{date.year}}</h4>
      </div>
      <div>
        <table>
          <thead>
            <tr>
              <th>STT</th>
              <th>Mã KH</th>
              <th>Tên KH</th>
              <th>Số hợp đồng</th>
              <th>Địa chỉ</th>
              <th colspan="2">{{date.month}}/{{date.year}}</th>
            </tr>
            <tr>
              <th />
              <th />
              <th />
              <th />
              <th />
              <th>Tiêu thụ</th>
              <th>Chỉ số cuối</th>
            </tr>
          </thead>
          <tbody>
            {{#each root}}
              <tr>
                <td>{{index}}</td>
                <td colspan="3">{{name}}</td>
                <td>{{totalMeters}}</td>
                <td>{{totalConsumption}}</td>
                <td />
              </tr>
                {{#with this}}
                    {{#each clients}}
                      <tr>
                        {{#with this}} 
                          <td>{{index}}</td>
                          <td>{{code}}</td>
                          <td>{{name}}</td>
                          <td>{{contractNo}}</td>
                          <td>{{address}}</td>
                          <td>{{consumption}}</td>
                          <td>{{terminalMeterNumber}}</td>
                        {{/with}}
                      </tr>
                    {{/each}}
                {{/with}}
            {{/each}}
          </tbody>
        </table> 
      </div>
    </div>
  </body>
</html>
