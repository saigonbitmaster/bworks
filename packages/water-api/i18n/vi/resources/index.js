const clientregisters = require('./clientregisters');
const clients = require('./clients');
const dmas = require('./dmas');
const providers = require('./providers');
const formulas = require('./formulas');

module.exports = {
  clientregisters,
  clients,
  dmas,
  providers,
  formulas,
};
